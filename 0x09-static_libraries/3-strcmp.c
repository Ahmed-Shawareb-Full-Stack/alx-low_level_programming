#include "main.h"
#include<string.h>

/**
 * _strcmp - return int depends on the result of cmp
 * @s1: first string
 * @s2: second string
 * Return: int
 */

int _strcmp(char *s1, char *s2)
{
	int i = strcmp(s1, s2);

	return (i);
}
